import { Component } from '../../index'

export class Form extends Component {
  template() {
    return (
      `<form
        id='form__main'
        className='auth-bar-form'>
      </form>`
    )
  }
}
